from statistics import mean, median, multimode

lista_liczb = []

liczba = float(input('Wprowadz liczbe:   '))
print(f'Wpisales: {liczba}')

lista_liczb.append(liczba)

# petla for, petla while, if'y.....